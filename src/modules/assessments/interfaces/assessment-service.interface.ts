import type { AssessmentStatisticsDto, AssignQuestionDto, CreateExamDto, ExamAttemptDto, ExamDto, ExamListQueryDto, ExamQuestionDto, ExamResultDto, ExamReviewDto, ExamsListDto, ManualGradeAnswerDto, ReorderExamQuestionDto, SaveAnswerDto, StudentAnswerDto, UpdateExamDto } from "../dto/index.js";
export interface IAssessmentService {
  createExam(tenantId: string, data: CreateExamDto): Promise<ExamDto>; listExams(tenantId: string, query: ExamListQueryDto): Promise<ExamsListDto>; getExam(id: string, tenantId: string): Promise<ExamDto>; updateExam(id: string, tenantId: string, data: UpdateExamDto): Promise<ExamDto>; deleteExam(id: string, tenantId: string): Promise<void>; restoreExam(id: string, tenantId: string): Promise<ExamDto>;
  assignQuestion(examId: string, tenantId: string, data: AssignQuestionDto): Promise<ExamQuestionDto>; listExamQuestions(examId: string, tenantId: string): Promise<ExamQuestionDto[]>; removeQuestion(examId: string, poolId: string, tenantId: string): Promise<void>; reorderQuestions(examId: string, tenantId: string, questions: ReorderExamQuestionDto[]): Promise<ExamQuestionDto[]>;
  startAttempt(examId: string, userId: string, tenantId: string): Promise<ExamAttemptDto>; resumeAttempt(examId: string, userId: string, tenantId: string): Promise<ExamAttemptDto>; saveAnswer(attemptId: string, userId: string, tenantId: string, data: SaveAnswerDto): Promise<StudentAnswerDto>; clearAnswer(attemptId: string, answerId: string, userId: string, tenantId: string): Promise<void>; submitAttempt(attemptId: string, userId: string, tenantId: string): Promise<ExamReviewDto>; cancelAttempt(attemptId: string, userId: string, tenantId: string): Promise<ExamAttemptDto>;
  gradeAnswer(answerId: string, tenantId: string, data: ManualGradeAnswerDto): Promise<StudentAnswerDto>; getResult(attemptId: string, userId: string | null, tenantId: string): Promise<ExamResultDto>; getReview(attemptId: string, userId: string | null, tenantId: string): Promise<ExamReviewDto>; getStatistics(tenantId: string): Promise<AssessmentStatisticsDto>;
}
